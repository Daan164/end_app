<%= form_for(@vak) do |f| %>
  <% if @vak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vak.errors.count, "error") %> prohibited this vak from being saved:</h2>

      <ul>
      <% @vak.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :tot_punten %><br />
    <%= f.text_field :tot_punten %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
