<%= form_for(@vak_user) do |f| %>
  <% if @vak_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vak_user.errors.count, "error") %> prohibited this vak_user from being saved:</h2>

      <ul>
      <% @vak_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_vak %><br />
    <%= f.text_field :name_vak %>
  </div>
  <div class="field">
    <%= f.label :name_docent %><br />
    <%= f.text_field :name_docent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
