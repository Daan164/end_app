<h1>Listing uvaks</h1>

<table>
  <tr>
    <th>Name vak</th>
    <th>Name user</th>
    <th>Aantal uren</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @uvaks.each do |uvak| %>
  <tr>
    <td><%= uvak.name_vak %></td>
    <td><%= uvak.name_user %></td>
    <td><%= uvak.aantal_uren %></td>
    <td><%= link_to 'Show', uvak %></td>
    <td><%= link_to 'Edit', edit_uvak_path(uvak) %></td>
    <td><%= link_to 'Destroy', uvak, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Uvak', new_uvak_path %>
